---

creation_rules:
  - path_regex: ansible/.*\.sops\.ya?ml
    # Personal
    pgp: >-
      0xBE954AA0B2A13EAD

  - path_regex: cluster/.*\.ya?ml
    encrypted_regex: '((?i)(pass|secret($|[^N])|key|token|^data$|^stringData))'
    # Personal, Flux
    pgp: >-
      0xBE954AA0B2A13EAD,
      0xEF97AF748D596FE2

  - path_regex: (clusters|gitops)/.*\.sops\.ya?ml
    encrypted_regex: '((?i)(pass|secret($|[^N])|key|token|^data$|^stringData))'
    # Personal, Flux
    pgp: >-
      0xBE954AA0B2A13EAD,
      0xEF97AF748D596FE2
