apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: node-red-data
  namespace: selfhosted
spec:
  accessModes:
  - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: rook-ceph-block
