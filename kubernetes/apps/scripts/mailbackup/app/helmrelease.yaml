---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mailbackup
spec:
  chart:
    spec:
      chart: app-template
      version: 4.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-labs
        namespace: flux-system
  interval: 30m
  values:
    controllers:
      mailbackup:
        type: cronjob

        cronjob:
          schedule: "0 2 * * *"
          backoffLimit: 0
          concurrencyPolicy: Forbid
          failedJobsHistory: 1
          successfulJobsHistory: 0

        pod:
          restartPolicy: OnFailure
          securityContext:
            runAsUser: 65534
            runAsGroup: 65534
            runAsNonRoot: true

        containers:
          app:
            image:
              repository: ghcr.io/bjw-s-labs/getmail
              tag: 2025.5.23@sha256:2a7a96c93271b9169ff125f21ac6822492d47e5ff42a918cf17c86259c919c18
            command:
              - "/scripts/mailbackup.sh"

    persistence:
      config:
        type: secret
        name: mailbackup-secrets
        advancedMounts:
          mailbackup:
            app:
              - path: /config
      scripts:
        type: configMap
        name: mailbackup-scripts
        defaultMode: 0775
        advancedMounts:
          mailbackup:
            app:
              - path: /scripts/mailbackup.sh
                subPath: mailbackup.sh
      backup:
        type: nfs
        server: gladius.bjw-s.internal
        path: /mnt/tank/Backup/Bernd
        advancedMounts:
          mailbackup:
            app:
              - path: /data/nas-backup/Bernd/Email
                subPath: Email
