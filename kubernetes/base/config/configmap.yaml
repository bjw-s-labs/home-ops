---
apiVersion: v1
kind: ConfigMap
metadata:
  name: base-config
  namespace: flux-system
data:
  # Gateway IP's
  GATEWAY_MANAGEMENT: 10.1.1.1

  # Networks
  NETWORK_SERVERS_CIDR: 10.1.1.0/24
  NETWORK_IOT_SUBNET_PREFIX: "10.1.3"

  TIMEZONE: Europe/Amsterdam
