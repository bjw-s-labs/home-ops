---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-trakt-sync-config
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: ceph-block
