---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-config
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 512Mi

  storageClassName: ceph-block
