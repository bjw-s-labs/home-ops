---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: phpipam-
resources:
  - ../kah-common-chart
  - ./cronjob.yaml
patchesJson6902:
  - target:
      group: helm.toolkit.fluxcd.io
      version: v2beta1
      kind: HelmRelease
      name: common
    patch: |-
      - op: replace
        path: /metadata/name
        value: phpipam
patchesStrategicMerge:
  - helm-values.yaml
