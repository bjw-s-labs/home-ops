---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: common
spec:
  values:
    global:
      nameOverride: theme-park

    image:
      repository: ghcr.io/k8s-at-home/theme-park
      tag: v1.9.7@sha256:01bb227597532153b47d74666163993d0f01410ee75646b236f514cdd4fa6da4

    service:
      main:
        ports:
          http:
            port: 8080

    resources:
      requests:
        cpu: 15m
        memory: 64M
      limits:
        memory: 64M
