---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: common
spec:
  values:
    global:
      nameOverride: theme-park

    image:
      repository: ghcr.io/k8s-at-home/theme-park
      tag: v1.9.8@sha256:60293ae7a3cddb33620cb05ed1563df3bf66c2e71cd8d3d1cf012dc62db2710e

    service:
      main:
        ports:
          http:
            port: 8080

    resources:
      requests:
        cpu: 15m
        memory: 64M
      limits:
        memory: 64M
