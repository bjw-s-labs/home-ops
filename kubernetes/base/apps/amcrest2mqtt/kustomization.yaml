---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: amcrest2mqtt-
resources:
  - ../kah-common-chart
patchesJson6902:
  - target:
      group: helm.toolkit.fluxcd.io
      version: v2beta1
      kind: HelmRelease
      name: common
    patch: |-
      - op: replace
        path: /metadata/name
        value: amcrest2mqtt
patchesStrategicMerge:
  - helm-values.yaml
