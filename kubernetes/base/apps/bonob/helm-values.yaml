---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: common
spec:
  values:
    global:
      nameOverride: bonob

    image:
      repository: docker.io/simojenki/bonob
      tag: v0.6.2

    env:
      BNB_PORT: 4534

    service:
      main:
        ports:
          http:
            port: 4534
