---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: common
spec:
  values:
    global:
      nameOverride: dsmr-reader

    image:
      repository: xirixiz/dsmr-reader-docker
      tag: amd64-5.3.0-2022.06.01

    resources:
      requests:
        cpu: 49m
        memory: 298M
      limits:
        memory: 595M

    service:
      main:
        ports:
          http:
            port: 80
