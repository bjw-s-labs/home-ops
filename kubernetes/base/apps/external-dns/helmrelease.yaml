---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: externaldns
spec:
  interval: 5m
  chart:
    spec:
      chart: external-dns
      version: 6.5.6
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
      interval: 5m

  values:
    crd:
      create: true
