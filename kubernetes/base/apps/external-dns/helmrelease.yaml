---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: externaldns
spec:
  interval: 5m
  chart:
    spec:
      chart: external-dns
      version: 1.10.1
      sourceRef:
        kind: HelmRepository
        name: kubernetes-sigs-external-dns
        namespace: flux-system
      interval: 5m

  values:
    serviceMonitor:
      enabled: true
