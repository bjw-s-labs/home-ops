---
peers:
  - peer-address: ${GATEWAY_MANAGEMENT}
    peer-asn: 64512
    my-asn: 64512
address-pools:
  - name: default
    protocol: bgp
    avoid-buggy-ips: true
    addresses:
      - ${NETWORK_BGP_SVC_RANGE}
