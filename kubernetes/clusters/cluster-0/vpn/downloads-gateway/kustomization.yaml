---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vpn
resources:
  - ../../../../base/apps/pod-gateway
  - ./secret.sops.yaml
patchesJson6902:
  - target:
      group: helm.toolkit.fluxcd.io
      version: v2beta1
      kind: HelmRelease
      name: pod-gateway
    patch: |-
      - op: replace
        path: /metadata/name
        value: downloads-gateway
patchesStrategicMerge:
  - helm-values.yaml
