---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mariadb
spec:
  values:
    auth:
      database: phpipam
      username: phpipam
      existingSecret: phpipam-database

    primary:
      persistence:
        existingClaim: phpipam-mariadb

      resources: {}
