---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: "${APP}"
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.2.0
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  dependsOn:
    - name: qbittorrent

  values:
    controllers:
      main:
        annotations:
          secret.reloader.stakater.com/reload: ${APP}-secret,${APP}-pguser-${APP}
          configmap.reloader.stakater.com/reload: ${APP}-configmap

        pod:
          securityContext:
            runAsUser: ${RUN_AS_USER}
            runAsGroup: ${RUN_AS_GROUP}
            fsGroup: ${RUN_AS_GROUP}
            fsGroupChangePolicy: OnRootMismatch

        containers:
          main:
            image:
              repository: ghcr.io/autobrr/autobrr
              tag: v1.33.0
            env:
              AUTOBRR__CHECKFORUPDATES: "false"
              AUTOBRR__HOST: 0.0.0.0
              AUTOBRR__PORT: &port 7474
              AUTOBRR__LOGLEVEL: DEBUG
              AUTOBRR__DATABASETYPE: postgres
              AUTOBRR__POSTGRESHOST:
                valueFrom:
                  secretKeyRef:
                    name: "${APP}-pguser-${APP}"
                    key: host
              AUTOBRR__POSTGRESPORT: "5432"
              AUTOBRR__POSTGRESUSER:
                valueFrom:
                  secretKeyRef:
                    name: "${APP}-pguser-${APP}"
                    key: user
              AUTOBRR__POSTGRESPASS:
                valueFrom:
                  secretKeyRef:
                    name: "${APP}-pguser-${APP}"
                    key: password
              AUTOBRR__POSTGRESDATABASE:
                valueFrom:
                  secretKeyRef:
                    name: "${APP}-pguser-${APP}"
                    key: dbname

              AUTOBRR__SESSIONSECRET:
                valueFrom:
                  secretKeyRef:
                    name: autobrr-secret
                    key: session_secret
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 256Mi

    service:
      main:
        ports:
          http:
            port: *port

    ingress:
      main:
        enabled: true
        className: "internal-nginx"
        hosts:
          - host: "${HOSTNAME}"
            paths:
              - path: /
                service:
                  name: main
                  port: http
        tls:
          - hosts:
              - "${HOSTNAME}"

    persistence:
      config:
        enabled: true
        type: emptyDir
      config-file:
        type: configMap
        name: "${APP}-configmap"
        globalMounts:
          - path: /config/config.toml
            subPath: config.toml
            readOnly: true
