---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: grafana-headers
  namespace: network
spec:
  headers:
    customRequestHeaders:
      X-Script-Name: "X-Scope-OrgID"
    customResponseHeaders:
      X-Custom-Response-Header: "${CLUSTER_NAME}"