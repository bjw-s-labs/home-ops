---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: autoscan-config
  namespace: media
  labels:
    k10.kasten.io/backup: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 100Mi

  storageClassName: ceph-block
