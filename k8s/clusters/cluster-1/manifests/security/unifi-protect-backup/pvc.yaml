---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unifi-protect-backup-config
  namespace: security
  labels:
    k10.kasten.io/backup: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 100Mi

  storageClassName: ceph-block
