---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config-cluster-2
  namespace: downloads
  labels:
    app.kubernetes.io/name: &name radarr
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 15Gi

  storageClassName: ceph-blockpool
