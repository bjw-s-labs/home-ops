# ---
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: radarr-config
#   namespace: downloads
#   labels:
#     k10.kasten.io/backup: "true"
#     app.kubernetes.io/name: &name radarr
#     app.kubernetes.io/instance: *name
#     snapshot.home.arpa/enabled: "true"
# spec:
#   accessModes:
#     - ReadWriteOnce
# 
#   resources:
#     requests:
#       storage: 10Gi
# 
#   storageClassName: ceph-block-external
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config-nvme
  namespace: downloads
  labels:
    app.kubernetes.io/name: &name radarr
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi

  storageClassName: ceph-block-external-nvme
