---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: echo-server
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: echo-server
      version: 0.3.1
      sourceRef:
        kind: HelmRepository
        name: ealenn
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: ealen/echo-server
      tag: 0.5.1

    ingress:
      enabled: false
