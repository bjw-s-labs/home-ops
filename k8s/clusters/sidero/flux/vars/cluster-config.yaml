---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config
  namespace: flux-system
data:
  CLUSTER_NAME: sidero

  NETWORK_BGP_SVC_RANGE: 10.200.0.0/24

  # Gateway IP's for this VLAN network
  # Switched to L3 networking. BGP breaks since I kill this interface
  # GATEWAY_MANAGEMENT: 10.200.0.1
  GATEWAY_MANAGEMENT: 10.255.253.1

  CLUSTER_1_API_IP: 10.200.1.1

  # Loadbalancer IP's
  LB_SIDERO: 10.200.0.50
