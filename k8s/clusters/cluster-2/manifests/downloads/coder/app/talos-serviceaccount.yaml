apiVersion: talos.dev/v1alpha1
kind: ServiceAccount
metadata:
  name: talos
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: Merge
    reloader.stakater.com/match: "false"
spec:
    roles:
        - os:reader
