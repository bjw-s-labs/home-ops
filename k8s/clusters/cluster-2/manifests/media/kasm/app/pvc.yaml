---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kasm-data
  namespace: media
  labels:
    app.kubernetes.io/name: &name kasm
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 100Gi

  storageClassName: ceph-block
