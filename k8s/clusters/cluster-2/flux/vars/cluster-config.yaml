---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config
  namespace: flux-system
data:
  CLUSTER_NAME: cluster-2
  CLUSTER_ID: "2"

  TIMEZONE: "America/Los_Angeles"

  # Gateway IP's
  # Switched to L3 networking. BGP breaks since I kill this interface
  GATEWAY_MANAGEMENT: 10.200.2.1

    # Networks
  NETWORK_BGP_SVC_CIDR: 10.200.2.32/27
  NETWORK_K8S_POD_CIDR: "10.42.0.0/16"

  # Loadbalancer IP's
  LB_INGRESS: 10.200.2.50
  LB_OPEN_IP: 10.200.2.51     # Open IP Address
