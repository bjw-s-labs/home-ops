name: main

on:
  push:
    branches:
      - master
#    paths:
#      - './secrets/cluster-vars.env'
#      - './secrets/**.yaml'
    tags-ignore:
      - flux-*

env:
  DEBIAN_FRONTEND: "noninteractive"

jobs:
  main:
    runs-on:
      - self-hosted

    steps:
      - uses: actions/checkout@v2
        with:
          token: ${{ secrets.ACCESS_TOKEN }}

      - name: Unlock secrets
        env:
          GIT_CRYPT_KEY: ${{ secrets.GIT_CRYPT_KEY }}
        run: |
          echo "$GIT_CRYPT_KEY" | base64 -d > ./git-crypt-key
          git-crypt unlock ./git-crypt-key
          rm ./git-crypt-key

      - name: Generate Secrets
        run: |
          task generate-sealed-secrets

      - name: Commit and push secrets
        run: |
          if [[ -n $(git status --porcelain) ]]; then
            git config --global user.name "bjw-s"
            git config --global user.email "bjw-s@users.noreply.github.com"
            find ./deployments -name "sealedsecret-*.yaml" -exec git add "{}" \;
            git commit -sm "Auto update sealed-secrets" || exit 0
            git push
          fi
