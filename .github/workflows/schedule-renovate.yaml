---
name: Schedule - Run Renovate

concurrency: renovate-run

on:
  workflow_dispatch:
  schedule:
  - cron: '0/15 * * * *'
  push:
    paths:
    - ".github/workflows/schedule-renovate.yaml"
    - ".github/renovate-config.ts"
    - ".github/renovate.json5"
    branches:
    - main

jobs:
  renovate:
    runs-on: self-hosted
    steps:
    - name: Get bot token
      id: get-app-token
      uses: getsentry/action-github-app-token@v1
      with:
        app_id: ${{ secrets.RENOVATE_APP_ID }}
        private_key: ${{ secrets.RENOVATE_APP_PRIVATE_KEY }}

    - name: Check out repository
      uses: actions/checkout@v2
      with:
        token: ${{ steps.get-app-token.outputs.token }}

    - name: Run Renovate
      uses: renovatebot/github-action@v25.56.6
      env:
        LOG_LEVEL: debug
      with:
        configurationFile: .github/renovate-config.ts
        token: '${{ steps.get-app-token.outputs.token }}'
