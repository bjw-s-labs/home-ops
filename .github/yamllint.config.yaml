---
ignore: |
  .secrets/

extends: default

rules:
  truthy:
    allowed-values: ['true', 'false', 'on', 'yes']
  line-length: disable
  comments:
    min-spaces-from-content: 1
  indentation:
    spaces: 2
    indent-sequences: consistent
