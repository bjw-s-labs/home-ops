---
data_dir: /tmp
sources:
  journal_logs:
    type: journald
    journal_directory: /run/log/journal
sinks:
  vector_sink:
    type: vector
    address: "{{ vector_aggregator_addr }}:{{ vector_aggregator_port }}"
    compression: true
    version: "2"
    inputs:
      - journal_logs
