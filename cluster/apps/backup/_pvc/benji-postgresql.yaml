---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: benji-postgresql
  namespace: backup
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 8Gi

  storageClassName: rook-ceph-block
