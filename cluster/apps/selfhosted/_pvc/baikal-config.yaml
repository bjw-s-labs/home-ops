---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: baikal-config
  namespace: selfhosted
spec:
  accessModes:
  - ReadWriteOnce

  resources:
    requests:
      storage: 256Mi

  storageClassName: rook-ceph-block
