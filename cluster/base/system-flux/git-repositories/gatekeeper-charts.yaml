---
apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: gatekeeper-charts-git
  namespace: system-flux
spec:
  interval: 10m
  url: https://github.com/open-policy-agent/gatekeeper
  ref:
    tag: v3.3.0
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/charts
